# GrMrMoney - Платформа управления финансами модельного агентства

Веб-приложение для управления финансами модельного агентства с автоматическим созданием USDT TRC-20 кошельков для каждого направления работы (модель/проект).

## Ключевые возможности

- 🔐 **Безопасность**: Приватные ключи шифруются AES-256 с мастер-паролем пользователя
- 💼 **Управление направлениями**: Каждое направление (модель/проект) имеет свой USDT кошелек
- 💸 **USDT TRC-20**: Работа с USDT на блокчейне TRON
- 📊 **Мониторинг**: Отслеживание входящих и исходящих транзакций
- 🔑 **Seed Phrase**: Показывается один раз при создании кошелька
- 🚀 **Modern Stack**: Next.js 15, TypeScript, Prisma, TronWeb

## Технологический стек

- **Frontend + Backend**: Next.js 15 + TypeScript + App Router
- **Database**: PostgreSQL (через Prisma ORM)
- **Blockchain**: TRON (TronWeb для работы с кошельками)
- **UI**: Tailwind CSS + shadcn/ui
- **Auth**: NextAuth.js
- **Crypto**: AES-256 шифрование (crypto-js)

## Требования

- Node.js 18+
- PostgreSQL 14+
- npm или yarn

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd grmrmoney
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка базы данных

Создайте PostgreSQL базу данных:

```bash
createdb grmrmoney
```

### 4. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/grmrmoney"

# NextAuth
NEXTAUTH_SECRET="<generate-with-openssl-rand-base64-32>"
NEXTAUTH_URL="http://localhost:3000"

# TRON
TRON_NETWORK="shasta"  # "mainnet" для продакшена, "shasta" для тестов
TRON_GRID_API_KEY=""   # Опционально, получить на trongrid.io

# App
NODE_ENV="development"
```

Сгенерируйте `NEXTAUTH_SECRET`:

```bash
openssl rand -base64 32
```

### 5. Применение миграций Prisma

```bash
npx prisma migrate dev --name init
```

### 6. Генерация Prisma Client

```bash
npx prisma generate
```

### 7. Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## Структура проекта

```
grmrmoney/
├── prisma/
│   └── schema.prisma          # Схема базы данных
├── src/
│   ├── app/                   # Next.js App Router
│   │   ├── (auth)/            # Страницы аутентификации
│   │   ├── (dashboard)/       # Защищенные страницы
│   │   └── api/               # API endpoints
│   ├── components/            # React компоненты
│   │   ├── ui/                # Базовые UI компоненты
│   │   ├── wallet/            # Компоненты для работы с кошельками
│   │   ├── categories/        # Компоненты направлений
│   │   └── transactions/      # Компоненты транзакций
│   ├── contexts/              # React Context
│   │   └── WalletContext.tsx  # Контекст для управления кошельками
│   ├── lib/                   # Библиотеки и утилиты
│   │   ├── tron/              # Работа с TRON blockchain
│   │   ├── crypto/            # Шифрование
│   │   ├── auth.ts            # NextAuth конфигурация
│   │   └── db.ts              # Prisma client
│   └── types/                 # TypeScript типы
└── package.json
```

## Использование

### Первый запуск

1. Зарегистрируйтесь через страницу `/register`
2. Войдите в систему
3. Создайте первое направление (категорию)
4. Сохраните показанную seed phrase (12 слов) в надежном месте
5. Установите мастер-пароль для шифрования кошельков

### Создание направления

1. Перейдите в "Направления" → "Создать направление"
2. Укажите название и описание
3. Система автоматически создаст USDT TRC-20 кошелек
4. **ВАЖНО**: Seed phrase показывается только один раз!
5. Установите мастер-пароль для шифрования

### Отправка USDT

1. Откройте направление
2. Нажмите "Отправить USDT"
3. Введите адрес получателя и сумму
4. Подтвердите транзакцию
5. Дождитесь подтверждения в сети TRON

### Просмотр транзакций

- Все транзакции отображаются в разделе "Транзакции"
- Входящие транзакции автоматически отслеживаются
- Можно добавить описание к каждой транзакции

## Безопасность

### Что защищено

✅ Seed phrases шифруются AES-256 с мастер-паролем пользователя
✅ Расшифрованные ключи хранятся ТОЛЬКО в памяти (React Context) во время сессии
✅ Приватные ключи НИКОГДА не отправляются на сервер
✅ Подпись транзакций происходит на клиенте
✅ Пароли хранятся в БД как bcrypt hash

### Потенциальные риски

⚠️ Если пользователь забыл мастер-пароль → доступ к кошелькам потерян (можно восстановить через seed phrase)
⚠️ XSS атаки могут похитить ключи из памяти (минимизируется через CSP headers)
⚠️ Keylogger может перехватить мастер-пароль при вводе

### Рекомендации

- Используйте надежный мастер-пароль
- Храните seed phrase в надежном месте (бумага, сейф)
- Регулярно делайте backup зашифрованных кошельков
- Используйте для тестов TRON Shasta testnet

## Разработка

### Просмотр базы данных

```bash
npx prisma studio
```

### Создание новой миграции

```bash
npx prisma migrate dev --name <migration-name>
```

### Линтинг

```bash
npm run lint
```

### Сборка для продакшена

```bash
npm run build
npm start
```

## TRON Testnet (Shasta)

Для тестирования используйте TRON Shasta testnet:

1. Установите `TRON_NETWORK="shasta"` в `.env`
2. Получите бесплатные TRX на https://www.trongrid.io/shasta
3. Получите тестовые USDT через контракт Shasta

## Deployment

### Vercel

1. Подключите PostgreSQL базу данных (Vercel Postgres, Supabase, Railway)
2. Настройте переменные окружения
3. Деплой через Vercel CLI или GitHub интеграцию

### Docker

```bash
docker build -t grmrmoney .
docker run -p 3000:3000 grmrmoney
```

## Troubleshooting

### Ошибка подключения к БД

- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа пользователя БД

### TronWeb ошибки

- Проверьте подключение к интернету
- Убедитесь, что `TRON_NETWORK` установлен правильно
- Проверьте наличие TRX для оплаты комиссий (energy)

### Ошибки шифрования

- Убедитесь, что мастер-пароль корректен
- Проверьте целостность зашифрованных данных в БД

## Roadmap

- [ ] Export зашифрованных кошельков (backup)
- [ ] Import существующих кошельков
- [ ] Multi-signature кошельки
- [ ] История курса USDT/USD
- [ ] Отчёты и аналитика
- [ ] Telegram/Email уведомления
- [ ] Multi-tenant для других агентств
- [ ] Мобильное приложение

## Лицензия

MIT

## Поддержка

Для вопросов и предложений создавайте Issues в репозитории.
